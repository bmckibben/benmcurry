.navbar.navbar-inverse.navbar-fixed-top
  .container
    #navbar.navbar-right.pull-right
      %ul.nav.navbar-nav
        -if user_signed_in?
          %li
            =link_to 'Sign out', destroy_user_session_path, method: :delete 
        -else
          %li
            =link_to 'Login', new_user_session_path
